services:
  - type: web
    name: badvel-election-analytics
    runtime: node
    plan: free
    buildCommand: npm install && npm run seed && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.11.0
